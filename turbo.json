{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build:app": {
      "dependsOn": ["^build:app"],
      "env": [
        "BETTER_AUTH_*",
        "BLOB_*",
        "DATABASE_URL",
        "EVE_NEXT_*",
        "*_CONNECTOR_UID",
        "KERNEL_*",
        "LINQ_*",
        "NODE_ENV",
        "SECRET_ENCRYPTION_KEY",
        "VERCEL_*"
      ],
      "outputs": [".next/**", "!.next/cache/**", "next-env.d.ts"]
    },
    "build:vercel": {
      "dependsOn": ["db:migrate"],
      "env": [
        "BETTER_AUTH_*",
        "BLOB_*",
        "DATABASE_URL",
        "*_CONNECTOR_UID",
        "KERNEL_*",
        "LINQ_*",
        "NODE_ENV",
        "SECRET_ENCRYPTION_KEY",
        "VERCEL_*"
      ],
      "outputs": [".next/**", "!.next/cache/**", "next-env.d.ts"]
    },
    "db:migrate": {
      "cache": false,
      "env": ["DATABASE_URL_UNPOOLED"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"]
    },
    "dev:app": {
      "cache": false,
      "passThroughEnv": [
        "BETTER_AUTH_*",
        "BLOB_*",
        "DATABASE_URL",
        "*_CONNECTOR_UID",
        "KERNEL_*",
        "LINQ_*",
        "NODE_ENV",
        "SECRET_ENCRYPTION_KEY",
        "VERCEL_*"
      ],
      "persistent": true
    },
    "deploy:app": {
      "cache": false
    },
    "format:check": {
      "outputs": []
    },
    "knip": {
      "outputs": []
    },
    "lint:app": {
      "outputs": []
    },
    "start:app": {
      "cache": false,
      "passThroughEnv": [
        "BETTER_AUTH_*",
        "BLOB_*",
        "DATABASE_URL",
        "*_CONNECTOR_UID",
        "KERNEL_*",
        "LINQ_*",
        "NODE_ENV",
        "SECRET_ENCRYPTION_KEY",
        "VERCEL_*"
      ],
      "persistent": true
    },
    "test:app": {
      "cache": false,
      "env": ["REAL_PG", "REAL_PG_COMPOSE_PROJECT"],
      "passThroughEnv": ["NODE_OPTIONS", "VERIFY_REPO_ROOT", "VERIFY_RUN_ID"],
      "outputs": []
    },
    "types:generate": {
      "passThroughEnv": ["NODE_OPTIONS", "VERIFY_REPO_ROOT", "VERIFY_RUN_ID"],
      "outputs": [".next/types/**", "next-env.d.ts"]
    },
    "types:check:app": {
      "dependsOn": ["types:generate"],
      "outputs": ["*.tsbuildinfo"]
    }
  }
}
