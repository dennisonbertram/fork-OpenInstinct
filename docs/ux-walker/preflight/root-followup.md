# Focused follow-up observations

Target http://localhost:3000, source f11a3a1, synthetic local account, 2026-09-05.

- Personal info Save is reachable by scrolling. `scrollintoview` followed by a settled screenshot placed Save at y=787.58, height 32 within the 844px viewport. Viewed `personal-save-reachable.png`; this clears the baseline uncertainty. The development Next overlay partly overlaps it, not a production control. No values saved.
- Confirmed medium flow defect: mobile navigation leaves the sheet open after choosing a destination. From Personal info, Toggle Sidebar -> All chats changed URL to /chat/history and active navigation but left the modal covering content. Reproduced from All chats -> Tasks after a two-second settling period; URL /tasks, active Tasks, sheet still covers screen. Escape reveals destination, one extra dismissal per navigation. Viewed `mobile-nav-after-click.png` and `mobile-nav-tasks-overlay.png`; `mobile-navigation.webm` records reproduction. Recording restart resets the page, so an initial stale-ref attempt failed before the successful recorded sequence. This is a flow/state change and should be filed, not bundled with layout quick fixes.

- Model chooser: Choose -> search `no-such-model-ux-audit` -> No matching models -> Escape returns Workspace. Viewed model-no-results.png; no product geometry violations (only invisible development panels). No model changed.
- Chat starter Make a plan filled and focused draft without sending; viewed chat-starter.png. Replaced draft with synthetic prompt requesting exactly Ready for review and no tools, then Submit. Route became /chat/wrun_01M1RSYHGBCR2BNKSET0W1HAM0. Settled UI showed only user bubble, no progress/error/assistant, usage 14.2K tokens; reloaded and same result remained. Viewed chat-result.png and chat-reloaded.png. Sent follow-up asking for short greeting; Stop appeared while pending and eventual assistant Hi! appeared. Viewed chat-followup.png and chat-followup-mobile.png. First-send expectation failed, follow-up delivery recovered; cause unknown, not diagnosed as credits. No JS errors. Mobile conversation geometry clean apart from invisible Agentation diagnostics.
