# STORY-023 — Inspect category validation and cancellation

Status: **pass for exercised empty-form paths and card mobile reachability**.

Contact info → Add contact → Save contact with blanks produced inline name/contact-value errors (`contact-errors.png`). Contact info back and Close returned to categories. Addresses → Add address opened the empty address form (`address.png`), then Addresses back and Close returned without saving. Cards → Add card → Save card with blanks produced field errors (`card-errors.png`). Mobile card screenshot shows responsive single-column fields (`card-mobile.png`); Cards back and Close worked. No card/address/contact record was created, and subsequent category triggers still reported empty.

Geometry: form spill 16px on each audited form corresponds to footer extension; no horizontal page overflow or wrapped controls. Desktop contact/address screenshots and mobile card screenshot show lower form/footer content below the visible dialog region. This requires scroll verification, which was not completed; do not claim all lower mobile controls were tested. One immediate click after closing Addresses encountered the closing overlay; repeating after the transition succeeded. No source diagnosis made.

Flow Log: catalog ideal 6 counts only category+add twice for three categories and is not a complete cancellation/validation benchmark. Actual semantic sequence 14 actions: category/add/save/back/close for contact (5), category/add/back/close for address (4), and category/add/save/back/close for card (5). One retry after transition adds 1 automation click. No values were entered; no secret capture. Complete saves and mobile bottom-control reachability remain unverified.

All listed screenshots were opened and visually inspected. Walk date: 2026-09-05. Browser: `ux-walker-openinstinct`, http://localhost:3000; desktop 1280×800 and mobile 390×844. No application source was read during the walk. Only designated synthetic account data was used. Invisible Agentation panels are excluded from geometry conclusions. Batch JavaScript errors were empty. Status-only network evidence is in `../STORY-024/snapshots/batch-network.txt`; it contains earlier shared-session requests too and is not a count of this story's actions. No network bodies or vault plaintext were captured.

## Settled mobile follow-up

The lower card controls are now verified. After scrolling the actual `[role=dialog]` container and taking a later settled capture, `screenshots/card-mobile-container-scroll.png` shows Billing ZIP / postal and the full Save card button. Its measured top/bottom were 796.05/828.05 within the 844px viewport; dialog scrollTop was 59. No layout defect is inferred from earlier page-level scroll attempts, which did not target this container reliably. A blank mobile Save card activation also produced validation errors; no values or records were saved. Additional captures `card-mobile-scrolled.png`, `card-mobile-wheel.png`, `card-mobile-after-click.png`, and `card-mobile-final.png` were viewed and retained to distinguish unsettled or insufficiently scrolled states from the final result. Address/contact lower mobile controls were not individually re-walked.
