# STORY-006 — Find what is available in a new workspace

Status: **pass for unavailable-state primary path**. Evidence captured by the coordinator/baseline walker on 2026-09-05 at `http://localhost:3000`, source `f11a3a1`, designated synthetic local account. This report consolidates those observations; its author did not perform a new browser run. All linked decisive PNGs were opened and visually inspected.

## Observed walk

The baseline opened Workspace and inspected desktop 1280×800 and mobile 390×844 captures. Channels shows enabled WebChat and disabled iMessage. Google Workspace and Square each show Admin setup needed with setup prose. Infrastructure shows Kernel browser, Vercel Blob, and AI Gateway model with a Choose action. The mobile setup text wraps narrowly beside the badges but no product geometry defect was confirmed on this surface.

## Flow log and limits

This was inspection of one Workspace surface; no integration or model was changed. The required unavailable-state information is visible. The Kernel Connected badge is presentation, not measured runtime health. Configured connector, image-storage-absent, and enabled messaging variants were not exercised. No new step count or completion time is inferred from screenshots. Baseline reported no JavaScript errors and no failed completed requests in its inspected request list.

Evidence: `../../preflight/baseline.md` and the two copied screenshots.
