diff --git a/dist/adapter.d.ts b/dist/adapter.d.ts
index e9648068188bdb91c358af16d614a445b25cdda6..8af7dc3daf59784aefda05ee1cccd182c67cdc53 100644
--- a/dist/adapter.d.ts
+++ b/dist/adapter.d.ts
@@ -17,6 +17,7 @@ export declare class SendblueAdapter implements Adapter<SendblueThreadId, Sendbl
     disconnect(): Promise<void>;
     encodeThreadId(data: SendblueThreadId): string;
     decodeThreadId(threadId: string): SendblueThreadId;
+    isDM(threadId: string): boolean;
     handleWebhook(request: Request, options?: WebhookOptions): Promise<Response>;
     private processInboundMessage;
     private handleTypingWebhook;
diff --git a/dist/index.js b/dist/index.js
index 3c8c8d8cff337f1520b79ca76d0e48422c746707..58849f10cc69865a61d6f0f950e7b66a450b1ea0 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -56,7 +56,8 @@ var SendblueAdapter = class {
     this.logger = config.logger ?? new ConsoleLogger();
     this.sdk = new SendblueAPI({
       apiKey: config.apiKey,
-      apiSecret: config.apiSecret
+      apiSecret: config.apiSecret,
+      maxRetries: 0
     });
   }
   // ---------------------------------------------------------------------------
@@ -99,6 +100,9 @@ var SendblueAdapter = class {
       contactNumber: Buffer.from(parts[2], "base64url").toString()
     };
   }
+  isDM(threadId) {
+    return !this.decodeThreadId(threadId).groupId;
+  }
   // ---------------------------------------------------------------------------
   // Webhook handling
   // ---------------------------------------------------------------------------
@@ -125,14 +129,6 @@ var SendblueAdapter = class {
     }
     if ("message_handle" in body && typeof body.message_handle === "string") {
       const payload = body;
-      this.logger.info("Sendblue webhook message received", {
-        is_outbound: payload.is_outbound,
-        status: payload.status,
-        hasContent: !!payload.content,
-        hasMediaUrl: !!payload.media_url,
-        mediaUrlPrefix: payload.media_url?.slice(0, 50),
-        service: payload.service
-      });
       if (!this.isServiceAllowed(payload.service)) {
         this.logger.debug("Sendblue webhook filtered by service", {
           service: payload.service
@@ -152,8 +148,6 @@ var SendblueAdapter = class {
   async processInboundMessage(payload, options) {
     if (!this.chat) return;
     const threadId = this.threadIdFromPayload(payload);
-    this.markRead(threadId).catch(() => {
-    });
     const factory = async () => {
       return this.parseMessage(payload);
     };
@@ -201,6 +195,7 @@ var SendblueAdapter = class {
   // ---------------------------------------------------------------------------
   async postMessage(threadId, message) {
     const decoded = this.decodeThreadId(threadId);
+    this.assertConfiguredLine(decoded);
     const text = this.renderOutbound(message);
     if (!text?.trim()) {
       this.logger.debug("Skipping empty outbound message");
@@ -234,6 +229,7 @@ var SendblueAdapter = class {
   }
   async sendMediaMessage(threadId, mediaUrl, content) {
     const decoded = this.decodeThreadId(threadId);
+    this.assertConfiguredLine(decoded);
     if (decoded.groupId) return;
     await this.sdk.messages.send({
       number: decoded.contactNumber,
@@ -292,6 +288,7 @@ var SendblueAdapter = class {
   // ---------------------------------------------------------------------------
   async addReaction(threadId, messageId, emoji) {
     const decoded = this.decodeThreadId(threadId);
+    this.assertConfiguredLine(decoded);
     const emojiName = typeof emoji === "string" ? emoji : emoji.name;
     const reaction = this.resolveReaction(emojiName);
     if (!reaction) {
@@ -352,6 +349,7 @@ var SendblueAdapter = class {
   // ---------------------------------------------------------------------------
   async startTyping(threadId) {
     const decoded = this.decodeThreadId(threadId);
+    this.assertConfiguredLine(decoded);
     if (!decoded.contactNumber) {
       this.logger.debug(
         "Sendblue typing indicators not supported for group threads"
@@ -380,6 +378,7 @@ var SendblueAdapter = class {
   // ---------------------------------------------------------------------------
   async markRead(threadId) {
     const decoded = this.decodeThreadId(threadId);
+    this.assertConfiguredLine(decoded);
     if (!decoded.contactNumber) return;
     await this.sdk.post("/api/mark-read", {
       body: {
@@ -416,6 +415,9 @@ var SendblueAdapter = class {
   // ---------------------------------------------------------------------------
   renderOutbound(message) {
     if (typeof message === "string") return toPlainText(message);
+    if ("raw" in message && typeof message.raw === "string") {
+      return toPlainText(message.raw);
+    }
     if ("markdown" in message && typeof message.markdown === "string") {
       return toPlainText(message.markdown);
     }
@@ -427,6 +429,11 @@ var SendblueAdapter = class {
     }
     return "";
   }
+  assertConfiguredLine(decoded) {
+    if (decoded.fromNumber !== this.config.defaultFromNumber) {
+      throw new Error("Sendblue thread sender does not match the configured line.");
+    }
+  }
   threadIdFromPayload(payload) {
     const fromNumber = payload.sendblue_number ?? (payload.is_outbound ? payload.from_number : payload.to_number);
     if (payload.group_id && payload.group_id.length > 0) {
